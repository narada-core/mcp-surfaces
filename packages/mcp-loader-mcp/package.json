{
  "name": "@narada-core/mcp-loader-mcp",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "type": "module",
  "description": "Native policy-gated runtime MCP surface loader and proxy.",
  "scripts": {
    "build": "pnpm run build:native",
    "build:native": "tsx scripts/build-native.ts",
    "typecheck": "tsc -b --pretty false",
    "test": "pnpm run test:native",
    "test:native": "cargo test --locked --manifest-path native/Cargo.toml && pnpm run test:native:harness",
    "test:native:harness": "pnpm run build:native && pnpm --filter @narada-core/mcp-e2e-harness build && pnpm run typecheck && node --test dist/test/native-native-children.test.js dist/test/epistemic-sequence-fabric.e2e.test.js"
  },
  "devDependencies": {
    "@narada-core/mcp-e2e-harness": "workspace:*",
    "@narada-core/mcp-runtime-proxy": "workspace:*",
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  },
  "naradaRuntimeArtifacts": {
    "win32": ["./dist/native/current.json", "./dist/native/versions/*"],
    "linux": ["./dist/native/current.json", "./dist/native/versions/*"],
    "darwin": ["./dist/native/current.json", "./dist/native/versions/*"]
  },
  "files": [
    "dist/native/"
  ]
}
