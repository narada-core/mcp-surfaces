{
  "name": "@narada-core/ledger-domain-mcp",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "description": "Generic narada.ledger-domain.v1 engine: hosts one static domain descriptor as a complete event-ledger MCP surface.",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "build:native": "tsx scripts/build-native.ts",
    "test:native": "cargo test --locked --manifest-path native/Cargo.toml",
    "test": "pnpm run test:bun",
    "test:bun": "pnpm run test:native && pnpm run build:native && pnpm --filter @narada-core/mcp-e2e-harness build && pnpm build && bun dist/test/protocol-smoke.test.js",
    "test:node": "pnpm run test:native && pnpm run build:native && pnpm --filter @narada-core/mcp-e2e-harness build && pnpm build && node --disable-warning=ExperimentalWarning dist/test/protocol-smoke.test.js"
  },
  "devDependencies": {
    "@narada-core/mcp-e2e-harness": "workspace:*",
    "@narada-core/mcp-runtime-proxy": "workspace:*",
    "@types/node": "^22.15.3",
    "tsx": "^4.21.0",
    "typescript": "^5.8.3"
  },
  "naradaRuntimeArtifacts": {
    "win32": ["./dist/native/current.json", "./dist/native/versions/*"],
    "linux": ["./dist/native/current.json", "./dist/native/versions/*"],
    "darwin": ["./dist/native/current.json", "./dist/native/versions/*"]
  },
  "files": [
    "dist/",
    "native/",
    "README.md"
  ]
}
