{
  "name": "@narada-core/mcp-materialization-contract-native",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "description": "Shared Rust authority for carrier materialization ownership, canonicalization, and fingerprints.",
  "scripts": {
    "test": "cargo test --locked --manifest-path native/Cargo.toml"
  },
  "files": ["contracts/", "native/Cargo.toml", "native/Cargo.lock", "native/src/", "README.md"]
}
