# Historical TypeScript delegation defects retired

This register described the removed TypeScript delegation implementation. It is retained only as a historical marker; the implementation no longer exists in this workspace. New defects belong with the native Rust delegation tests under `packages/shared/mcp-surfaces-native/native`.
